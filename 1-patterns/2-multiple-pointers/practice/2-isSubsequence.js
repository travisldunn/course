// ----> Check if the first string is a subsequence (in order) of the second string

isSubsequence = (str1, str2) => {};

console.log(isSubsequence("hello", "hoooello world")); //true
console.log(isSubsequence("hdo", "hossllo")); //false
console.log(isSubsequence("hdo", "hdo")); //true
console.log(isSubsequence("abc", "acb")); //false
