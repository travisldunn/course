class Node {}

class Queue {}

let queue = new Queue();

queue.enqueue("mother");
queue.enqueue("fucken");
queue.enqueue("shit");
queue.enqueue("bricks");

console.log(queue.dequeue());
console.log(queue.dequeue());
console.log(queue.dequeue());
console.log(queue.dequeue());
